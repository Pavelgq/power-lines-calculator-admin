import { ActionColumnParamsI, ActionFullInterface } from "../../../interfaces/action.interface";

export interface TableCollapsibleRowProps {
  actionData: ActionFullInterface;
  columns: ActionColumnParamsI[];
}
